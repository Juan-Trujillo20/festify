spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/festify
    username: user
    password: secret
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate.format_sql: true
    open-in-view: false
springdoc:
  api-docs:
    path=/api-docs:
server:
  port: 8081